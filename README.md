# Database-Tools
The app runs a School Management Database entirely in the browser using sql.js (SQLite compiled to WebAssembly). Users can browse four tables — students, teachers, courses, and enrollments — and query them via a visual builder or by writing raw SQL directly.  
